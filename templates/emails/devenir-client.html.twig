<h1 class="text-green-600">Nouvele demande pour devenir client :</h1>
<small>{{ contact.createdAt | date('d F Y') | format_date(locale='fr') }} à {{ comment.createdAt | date('H:i') }}</small>
<br/>
<p>Nom/Prénom : {{ contact.fistName }} {{ contact.lastName }}</p>
<p>Email : {{ contact.email }}</p>
<p>Téléphone : {{ contact.phone }}</p>
</br>
<p>Société : {{ contact.society }}</p>
<p>Status juridique : {{ contact.legalStatus }}</p>
<p>Nombre de salariés : {{ contact.numberEmployees }}</p>
<p>Localité du siége : {{ contact.localitySociety }}</p>
<p>Rôle dans l'entreprise : {{ contact.position }}</p>
<br/>
<p>Cet email provient du formulaire de contact de fiduciaire-genevoise.ch.</p>